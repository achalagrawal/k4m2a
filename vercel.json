{
  "installCommand": "yarn install",
  "buildCommand": "yarn intl:compile && yarn expo export:web",
  "outputDirectory": "web-build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}